
// const batteryBatches = [4, 5, 3, 4, 4, 6, 5].reduce(totalBatteries, 0);

// function totalBatteries(accumulator, a) {
    
//     return accumulator + a;
//     }

// console.log(batteryBatches);

const totalBatteries = [4, 5, 3, 4, 4, 6, 5].reduce(batteryBatches, 0);

function batteryBatches(accumulator, a) {
    
    return accumulator + a;
    }

console.log(totalBatteries);